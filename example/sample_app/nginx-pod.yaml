apiVersion: v1
kind: Pod 
metadata:
  name: test-autoscaler
spec:
  containers:
  - name: test-autoscaler
    image: nginx
    resources:
      limits:
        cpu: "3000m"
      requests:
        cpu: "3000m"
